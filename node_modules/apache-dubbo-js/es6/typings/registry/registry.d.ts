import Context from '../context';
import DubboUrl from '../dubbo-url';
import { IRegistrySubscriber } from '../types';
export declare type TAgentAddr = string;
export declare type TDubboInterface = string;
/**
 * 抽取注册中心的基类
 */
export default class Registry<T = {}> {
    protected _props: T;
    protected _subscriber: IRegistrySubscriber;
    protected readonly _dubboServiceUrlMap: Map<TDubboInterface, Array<DubboUrl>>;
    constructor(props: T);
    /**
     * 订阅者
     * @param subscriber
     */
    subscribe(subscriber: IRegistrySubscriber): this;
    /**
     * 获取可以处理上下文context中的dubbo接口信息map
     * @param ctx
     */
    getAgentAddrMap(ctx: Context): {
        [name: string]: DubboUrl;
    };
}
