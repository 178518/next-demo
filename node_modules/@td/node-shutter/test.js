const Shutter = require('./lib/index').default;

const shutter = new Shutter({
  hostList: '10.57.17.39:8188',
  application: 'prelude',
  group: 'DEFAULT_GROUP',
  environment: 'dev',
  SecretKey: 'SecretKey',
  AccessKey: 'AccessKey'
});

shutter.addProperties('prelude', ['prelude.properties']);

shutter.fetchRemoteData().then(() => {
  shutter.addWatch('prelude');
});