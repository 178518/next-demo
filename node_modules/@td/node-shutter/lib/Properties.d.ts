/// <reference path="../typings/index.d.ts" />
import { IConfig } from './IConfig';
export interface IProperties {
    app: string;
    config: IConfig;
    locations: string[];
}
export declare class Properties {
    private app;
    private config;
    private properties;
    private locations;
    private hash;
    private watchHandle;
    constructor(options: IProperties);
    getRemoteUrl(location: string): string;
    getProperty(key: string): any;
    setProperty(key: string, value: any): void;
    observer(): Promise<void>;
    watch(handle: void): void;
    getCacheFilePath(location: string): string;
    cacheFile(text: string, location: string): void;
    readCacheFile(location: string): string;
    parseToAssign(text: string): Promise<{}>;
    getQueryHeaders(plainText: string, timestamp: number): any;
    fetchData(): Promise<boolean | void>;
    getApp(): string;
    getLocations(): string[];
    addLocations(locations: string[]): void;
}
