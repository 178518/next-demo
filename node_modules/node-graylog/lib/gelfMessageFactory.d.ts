export declare const LEVEL_MAPPING: {
    [key: string]: number;
};
export interface LoggerOption {
    file: string;
    app: string;
    logger: string;
    host: string;
    marker?: string;
    thread?: string;
    requestUri?: string;
    env?: string;
    cluster?: string;
}
export default class GelfMessageFactory {
    private file;
    private app;
    private host;
    private logger;
    private level;
    private marker?;
    private thread?;
    private requestUri?;
    private env?;
    private cluster?;
    constructor({ file, app, logger, marker, thread, requestUri, host, env, cluster }: LoggerOption);
    createGelfMessage(msg: string, level: string): string;
}
